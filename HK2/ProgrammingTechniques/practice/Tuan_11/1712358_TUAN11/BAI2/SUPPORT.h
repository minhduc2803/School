#ifndef _SUPPORT_H_
#define _SUPPORT_H_
#include <math.h>
#include <stdio.h>
//if x is a prime number then return true, else retun false
bool TestPrime(int x);
//if x is a symmetry number the return true, else return false
bool TestSymmetry(int x);
//sorting the array
void Sorting(int A[], int N);

#endif
